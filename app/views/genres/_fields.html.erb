<% @genres.each do |genre| %>
  <tr>
    <td><%= genre.name %></td>
    <td><%= genre.movies.count %></td>
    <td><%= link_to 'edit', edit_genre_path(genre) %></td>
    <td><%= link_to 'delete', genre_path(genre), method: :delete %></td>
  </tr>
<% end %>
